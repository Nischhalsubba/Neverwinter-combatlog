{
  "name": "strikeglass",
  "private": true,
  "version": "0.6.0",
  "description": "A local-first, dual-engine Neverwinter combat-log analyzer with verified combat values, indexed encounter scopes, raw-hit traceability, player comparison, party rotation, and lightweight Canvas charts.",
  "author": "Nischhal Raj Subba",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "start": "python -m http.server 5173",
    "dev": "python -m http.server 5173",
    "test": "node scripts/smoke-test.mjs && node scripts/parser-regression.mjs && node scripts/verification-regression.mjs && node scripts/scope-regression.mjs",
    "build": "pnpm test && node build-static.mjs",
    "check": "pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "type": "module"
}
