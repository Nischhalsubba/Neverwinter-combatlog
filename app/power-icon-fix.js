(function(){
  const short=v=>window.NWMeta&&NWMeta.compact?NWMeta.compact(v):String(v);
  function label(power,cat){return (window.NWAssets?NWAssets.powerHtml(power,cat,'powerIcon exactPowerIcon'):'')+'<span>'+String(power||'')+'</span>'}
  if(window.NWMeta){NWMeta.powerLabel=p=>label(p,NWParser.category(p));NWMeta.resolvePower=p=>{const cat=NWParser.category(p);return{power:p,category:cat,iconUrl:window.NWAssets?NWAssets.powerUrl(p,cat):null,icon:'?',color:'#8796ff'}}}
  if(typeof bars==='function')bars=function(title,items,key='power',alt=''){const mx=Math.max(1,...items.map(x=>x.damage));return '<h3>'+title+'</h3><div class="bars">'+items.map(x=>{const l=key==='power'?label(x[key],x.category):x[key];return '<div class="barrow rich"><span>'+l+'</span><div class="bar '+alt+'"><i style="width:'+Math.max(2,x.damage/mx*100)+'%"></i></div><b>'+((x.share||0).toFixed(1))+'%</b><em>'+short(x.damage)+'</em></div>'}).join('')+'</div>'};
  if(typeof renderDamage==='function')renderDamage=function(rows,pid){const ps=NWParser.powers(rows,pid);let html='<section class="panel"><h3>Damage Out - '+ps.length+' Powers</h3><div class="table"><table><thead><tr><th>Power</th><th>Category</th><th>Hits</th><th>Damage</th><th>%</th><th>Avg</th><th>Max</th><th>Crit%</th></tr></thead><tbody>';html+=ps.map((p,i)=>'<tr class="powerRow" data-power="'+i+'"><td>› <b>'+label(p.power,p.category)+'</b></td><td><span class="badge">'+p.category+'</span></td><td>'+Math.round(p.hits).toLocaleString()+'</td><td>'+short(p.damage)+'</td><td>'+((p.share||0).toFixed(1))+'%</td><td>'+short(p.avg)+'</td><td>'+short(p.max)+'</td><td>'+((p.crit||0).toFixed(1))+'%</td></tr>').join('');html+='</tbody></table></div><div id="raw"></div></section>';document.querySelector('#content').innerHTML=playerHeader()+html;document.querySelectorAll('[data-power]').forEach(tr=>tr.onclick=()=>renderRaw(ps[+tr.dataset.power]));};
})();
