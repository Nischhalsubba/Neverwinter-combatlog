{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Nexus Combat Analyzer desktop app.",
  "windows": ["main", "live-widget"],
  "permissions": ["core:default", "dialog:default", "dialog:allow-open"]
}
